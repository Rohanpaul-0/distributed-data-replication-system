[project]
name = "replicator-control-plane"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "sqlalchemy>=2.0",
  "pydantic>=2.0",
  "prometheus-client>=0.20",
  "aiohttp>=3.9"
]
